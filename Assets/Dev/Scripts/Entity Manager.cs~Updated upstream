/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

<<<<<<< Updated upstream:Assets/Dev/Scripts/Entity Manager.cs
public class EntityManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
=======
public class TestScript : MonoBehaviour
{
    public int width, height;

    public GameObject prefab;


    int rowDownCount = 0;
    float StartOriginY = 0;

    private void Start()
    {
        for (int i = 0; i < height; i++)
        {
            for (int k = 0; k < width; k++)
            {
                Vector3 pos = new Vector3(k + rowDownCount, (k * 0.592f) + StartOriginY, 5);
                GameObject go = Instantiate(prefab, pos, Quaternion.identity);
                SpriteRenderer sr = go.GetComponent<SpriteRenderer>();


                sr.sortingOrder = width + (rowDownCount - k);
            }

            rowDownCount++;
            StartOriginY -= 0.592f;

            //StartOriginY = StartOriginY + 0.592f;
        }
    }



>>>>>>> Stashed changes:Assets/Dev/Scripts/TestScript.cs
}



*/

